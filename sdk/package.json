{
  "name": "@lifelox/dex-sdk",
  "version": "1.0.0",
  "description": "Lifelox DEX SDK — EIP-6963 wallet connect, PXC token support (both lanes), and router/swap builders",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "viem": "^2.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.3"
  }
}
